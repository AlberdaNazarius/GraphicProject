import { ifft } from 'fft-js';

export const fourierTransform = () => {

}




// export const inverseFourierTransform = (dftShifted, filter) => {
//   // Perform the inverse Fourier transform
//   const phasors = ifft(data);
//
//   // Convert the complex numbers to magnitudes
//   const magnitudes = phasors.map(phasor => Math.sqrt(phasor[0]**2 + phasor[1]**2));
//
//   return magnitudes;
// };